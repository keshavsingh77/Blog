// This service has been stripped of AI functionality as requested.
// It remains as a placeholder to prevent build errors if imported elsewhere,
// though all consumers should have been updated.

export const generateBlogPost = async (topic: string): Promise<any> => {
  throw new Error("AI features have been disabled.");
};
